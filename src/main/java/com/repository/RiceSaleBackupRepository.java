package com.repository;

import com.domain.RiceSaleBackup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RiceSaleBackupRepository extends JpaRepository<RiceSaleBackup, Long> {
}
